# FrameDiff Example Repository Instructions

- Use `Vikas Reddy <vikas@framediff.com>` for commit authors and committers.
- Keep example-specific compositions, assets, prompts, looks, and editorial choices here.
- Promote reusable behavior to `micromanager3000/framediff`; do not duplicate engine or Studio code.
- Update the pinned `vendor/framediff` submodule deliberately and validate before committing it.
- Run `npm run check` and `npm run build` before publishing changes.
- Do not add colored accent rules to the left edge of UI blocks.
